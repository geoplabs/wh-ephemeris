<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Yearly Forecast</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 24px; }
        h1, h2, h3 { color: #222; }
        .month { page-break-before: always; }
        .section { margin-bottom: 16px; }
        .bullets li { margin-bottom: 4px; }
        table { width: 100%; border-collapse: collapse; }
        th, td { border: 1px solid #ccc; padding: 6px; font-size: 12px; }
    </style>
</head>
<body>
    <h1>{{ data.report.meta.year }} Yearly Forecast</h1>
    <p>Generated at {{ data.generated_at }}</p>

    <h2>Year at a Glance</h2>
    <div class="section">
        <p>{{ data.report.year_at_glance.commentary }}</p>
        <ul>
            {% for ev in data.report.year_at_glance.top_events %}
            <li><strong>{{ ev.title }}</strong> ({{ ev.date }}) — {{ ev.summary }}</li>
            {% endfor %}
        </ul>
    </div>

    <h2>Eclipses &amp; Lunations</h2>
    <div class="section">
        <ul>
            {% for ec in data.report.eclipses_and_lunations %}
            <li>{{ ec.date }} — {{ ec.kind }}: {{ ec.guidance }}</li>
            {% endfor %}
        </ul>
    </div>

    {% for month in data.report.months %}
    <div class="month">
        <h2>{{ month.month }}</h2>
        <div class="section"><h3>Overview</h3><p>{{ month.overview }}</p></div>
        <div class="section"><h3>Career &amp; Finance</h3><p>{{ month.career_and_finance }}</p></div>
        <div class="section"><h3>Relationships &amp; Family</h3><p>{{ month.relationships_and_family }}</p></div>
        <div class="section"><h3>Health &amp; Energy</h3><p>{{ month.health_and_energy }}</p></div>
        <div class="section"><h3>Rituals &amp; Journal</h3><p>{{ month.rituals_and_journal }}</p></div>
        <div class="section"><h3>Planner Actions</h3>
            <ul class="bullets">
                {% for action in month.planner_actions %}
                <li>{{ action }}</li>
                {% endfor %}
            </ul>
        </div>
        <div class="section"><h3>High Score Days</h3>
            <ul>
                {% for ev in month.high_score_days %}
                <li>{{ ev.date }} — {{ ev.transit_body }} to {{ ev.natal_body }} ({{ ev.aspect }})</li>
                {% endfor %}
            </ul>
        </div>
        <div class="section"><h3>Caution Days</h3>
            <ul>
                {% for ev in month.caution_days %}
                <li>{{ ev.date }} — {{ ev.transit_body }} to {{ ev.natal_body }} ({{ ev.aspect }})</li>
                {% endfor %}
            </ul>
        </div>
        <div class="section"><h3>Aspect Grid</h3>
            <table>
                <thead><tr><th>Date</th><th>Transit</th><th>Natal</th><th>Aspect</th><th>Score</th></tr></thead>
                <tbody>
                    {% for row in month.aspect_grid %}
                    <tr>
                        <td>{{ row.date }}</td><td>{{ row.transit }}</td><td>{{ row.natal }}</td><td>{{ row.aspect }}</td><td>{{ row.score }}</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
    {% endfor %}

    <div class="section">
        <h2>Appendix A: All Events</h2>
        <table>
            <thead><tr><th>Date</th><th>Transit</th><th>Natal</th><th>Aspect</th><th>Score</th><th>Summary</th></tr></thead>
            <tbody>
                {% for ev in data.report.appendix_all_events %}
                <tr><td>{{ ev.date }}</td><td>{{ ev.transit_body }}</td><td>{{ ev.natal_body }}</td><td>{{ ev.aspect }}</td><td>{{ ev.score }}</td><td>{{ ev.user_friendly_summary }}</td></tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="section">
        <h2>Appendix B: Glossary</h2>
        <ul>
            {% for term, definition in data.report.glossary.items() %}
            <li><strong>{{ term }}</strong>: {{ definition }}</li>
            {% endfor %}
        </ul>
    </div>

    <div class="section">
        <h2>Appendix C: Interpretation Index</h2>
        <ul>
            {% for term, definition in data.report.interpretation_index.items() %}
            <li><strong>{{ term }}</strong>: {{ definition }}</li>
            {% endfor %}
        </ul>
    </div>
</body>
</html>
