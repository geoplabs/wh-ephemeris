<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <title>WhatHoroscope | Monthly Horoscope</title>
  <style>
    body {
      font-family: 'Poppins', 'Merriweather', serif;
      color: #212121;
      margin: 0;
      padding: 0;
      background: #ffffff;
    }
    .cover {
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      background: linear-gradient(180deg, #ffffff 0%, #f5f5f5 100%);
    }
    .cover img {
      width: 160px;
      margin-bottom: 1.2rem;
    }
    .cover h1 {
      color: #c21807;
      font-size: 2.8rem;
      margin-bottom: 0.4rem;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    .cover p {
      color: #555;
      font-size: 1.05rem;
    }
    .page {
      padding: 2.8rem 3.8rem;
      page-break-after: always;
    }
    h2 {
      color: #c21807;
      border-left: 4px solid #c21807;
      padding-left: 12px;
      text-transform: uppercase;
      letter-spacing: 0.4px;
    }
    .highlight {
      margin-bottom: 1rem;
    }
    .highlight strong {
      color: #c21807;
    }
    .calendar {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
      gap: 1rem 1.5rem;
    }
    .calendar-item {
      border: 1px solid #f0f0f0;
      border-radius: 12px;
      padding: 1rem;
      background: #fafafa;
      box-shadow: 0 4px 8px rgba(0,0,0,0.04);
    }
    .calendar-item span {
      display: block;
      font-weight: 600;
      color: #c21807;
      margin-bottom: 0.4rem;
    }
    .footer {
      text-align: center;
      font-size: 0.85rem;
      color: #777;
      margin-top: 2.5rem;
      border-top: 1px solid #eee;
      padding-top: 0.8rem;
    }
  </style>
</head>
<body>
  <div class="cover">
    <img src="{{ branding.logo_url or 'https://whathoroscope.com/logo.png' }}" alt="WhatHoroscope Logo" />
    <h1>{{ data.month_label }} {{ data.year }}</h1>
    <p>Curated guidance for {{ data.prepared_for }}</p>
    <p>Generated on {{ data.generated_at }}</p>
  </div>

  <div class="page">
    <h2>Key Highlights</h2>
    {% if data.highlights %}
      {% for entry in data.highlights %}
        <p class="highlight"><strong>{{ entry.date }} — {{ entry.headline }}</strong><br/>{{ entry.summary }}</p>
      {% endfor %}
    {% else %}
      <p>Celestial motions support a reflective month with space to harmonise priorities.</p>
    {% endif %}
  </div>

  <div class="page">
    <h2>Notable Dates</h2>
    <div class="calendar">
      {% for item in data.calendar %}
        <div class="calendar-item">
          <span>{{ item.date }}</span>
          {{ item.title }}<br/>
          <em style="font-size:0.85rem; color:#555; text-transform:capitalize;">{{ item.tone }} influence</em>
        </div>
      {% endfor %}
    </div>

    <div class="footer">
      © {{ data.year }} WhatHoroscope.com | All Rights Reserved
    </div>
  </div>
</body>
</html>
